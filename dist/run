#!/usr/bin/env ruby
require 'fileutils';

Dir['./*.html'].each do |file|
  next unless file['%']
  new_file = file.force_encoding('windows-1252').encode('utf-8')
  new_file = new_file.gsub('Â%92', '-')
  p new_file

  # FileUtils.move(file, new_file)
    # .encode('utf-8')
  # p file
  # p file[13]
  # puts file[13]
  # p file.gsub(file[13], 'kjkj')

end

